~ jolk.lang;

/// # Sequential
/// The Sequential protocol defines the contract for linear continuums.
///
/// <T> is the type of elements contained in the sequence.
///
/// @author Wouter Roose

public protocol Sequential<T> {
    T at(Int index);
    Self push(T element);
    T first();
    Int size();
}