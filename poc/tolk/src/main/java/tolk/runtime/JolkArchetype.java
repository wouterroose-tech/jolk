package tolk.runtime;

/// # JolkArchetype
///
/// Represents the structural archetype of a Jolk type.
/// The available archetypes are `CLASS`, `VALUE`, `RECORD`, `ENUM`, and `PROTOCOL`.
public enum JolkArchetype {
    CLASS,
    VALUE,
    RECORD,
    ENUM,
    PROTOCOL
}